import { UploadPostDirective } from './upload-post.directive';

describe('UploadPostDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadPostDirective();
    expect(directive).toBeTruthy();
  });
});
